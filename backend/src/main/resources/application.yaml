spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/lorenzoconsulting
    username: test
    password: test
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  liquibase:
    change-log: classpath:liquibase/master.xml
